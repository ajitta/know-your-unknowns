{
  "description": "field-guide — 구현 중 계획 이탈 기록(IMPLEMENTATION_NOTES.md) 리마인더. 세션당 1회, 파일 수정 N회(기본 10) 도달 시 발동. FIELD_GUIDE_NOTES_THRESHOLD 환경변수로 조정, 0이면 비활성화.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/impl_notes_reminder.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
