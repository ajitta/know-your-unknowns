{
  "description": "unknowns — reminder to record plan deviations during implementation (IMPLEMENTATION_NOTES.md). Fires when file edits reach N (default 10), once per session by default. UNKNOWNS_NOTES_THRESHOLD adjusts threshold (0=off); UNKNOWNS_NOTES_REPEAT=1 repeats at every threshold multiple. Legacy FIELD_GUIDE_NOTES_THRESHOLD also recognized.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/impl_notes_reminder.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
